stages:
  - test
  - build
  - deploy

include:
  - template: Dependency-Scanning.gitlab-ci.yml
variables:
  SECURE_LOG_LEVEL: debug
gemnasium-python-dependency_scanning:
  image:
    name: registry.gitlab.com/gitlab-org/security-products/analyzers/gemnasium-python:2-python-3.9

pre-commit:
  stage: test
  image: musicscience37/pipenv:v1.7
  script:
    - pipenv sync --dev
    - pipenv run pre-commit run --all-files
  needs: []

build:
  stage: build
  image: musicscience37/sphinx-doxygen:v3.3
  script:
    - pipenv sync
    - pipenv run make html
  artifacts:
    paths:
      - build
    expire_in: "1 week"
  needs: []

pages:
  stage: deploy
  image: ubuntu
  script:
    - mv build/html public
  artifacts:
    paths:
      - public
  only:
    - master
  needs: [build]
