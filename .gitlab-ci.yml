image: musicscience37/sphinx:version-1.1

stages:
  - build
  - deploy

build:
  stage: build
  script:
    - make html
  artifacts:
    paths:
      - build
    expire_in: '1 week'

pages:
  stage: deploy
  script:
    - mv build/html public
  artifacts:
    paths:
    - public
  only:
    - master
